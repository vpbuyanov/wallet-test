server:
  address: 0.0.0.0:9000

postgres:
  # 0.0.0.0 - to run in cmd
  # db_postgres_test - to run in docker
  host: db_postgres_test
  port: 5432
  user: postgres
  password: postgres
  dbname: wallet